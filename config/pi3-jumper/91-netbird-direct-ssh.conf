# Required OpenSSH listener for the Travel Router control relay.
# Install as /etc/ssh/sshd_config.d/91-netbird-direct-ssh.conf on pi3-jumper.
#
# Port 22 remains available for normal management. NetBird intercepts peer-IP
# TCP/22 for its interactive SSH service, so appliance controls use TCP/2222.
Port 22
Port 2222
